"""Pure-logic core for the D4Extract importer.

``sidecar`` is ``bpy``-free (unit-testable). ``alpha_fixup``,
``material_builder`` and ``variant_catalog`` are Blender-facing helpers
the operators build on.
"""
